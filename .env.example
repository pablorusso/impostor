## Redis Configuration ##
# Funciona con cualquier proveedor de Redis (RedisLabs, Upstash, local, etc.)
# Nota: Si no hay REDIS_URL configurada, la app usará memoria local para desarrollo
REDIS_URL=redis://default:password@host:port
# Ejemplo para Redis local:
# REDIS_URL=redis://localhost:6379
# Ejemplo para RedisLabs:
# REDIS_URL=redis://default:password@redis-12345.c1.us-east-1-2.ec2.cloud.redislabs.com:12345

## PUSHER CONFIGURATION ##
# Claves para el servidor
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=
# Claves para el cliente (frontend)
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=

# --- Development & Testing ---
# Si se establece a 'true', cada vez que se cree una nueva partida, se añadirán 3 jugadores "bot" para pruebas rápidas.
ADD_RANDOM_PLAYERS=false
